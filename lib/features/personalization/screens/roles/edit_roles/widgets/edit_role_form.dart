import 'package:flutter/material.dart';

class EditRoleFormScreen extends StatelessWidget {
  const EditRoleFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
