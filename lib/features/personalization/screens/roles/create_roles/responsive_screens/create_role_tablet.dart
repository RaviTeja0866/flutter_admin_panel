import 'package:flutter/material.dart';

class CreateRoleTabletScreen extends StatelessWidget {
  const CreateRoleTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
